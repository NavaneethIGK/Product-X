# AI Configuration (OPTIONAL - Use config.json instead for better control)
# This file is optional. Recommended approach: Use config.json
# Copy this file to .env and set values only if you need environment variable override

# OPENAI API KEY (Optional - leave empty if not using OpenAI)
OPENAI_API_KEY=

# GROQ API KEY (Optional - leave empty if not using Groq)
GROQ_API_KEY=

# PREFERRED AI PROVIDER (Optional - values: "groq" or "openai")
# If not set, config.json takes precedence
AI_PROVIDER=

